:root {
  --bg: #1f252e;
  --panel: #222a34;
  --panel-light: #2c3440;
  --text: #e8edf5;
  --muted: #96a0b5;
  --accent: #4caf50;
  --danger: #ef5350;
  --row-h: 60px;
  --sidebar-width: 380px;
  --card-max-width: calc(var(--sidebar-width, 380px) - 20px);
}
* { box-sizing: border-box; }
html, body { margin:0; padding:0; height:100%; overflow: hidden; }
body {
  background: var(--bg);
  color: var(--text);
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  display: flex;
  flex-direction: column;
}

/* Utility */
.hidden { display: none !important; }
.full-width { width: 100%; }
.mono-block { font-family: monospace; white-space: pre-wrap; font-size: 12px; color: #ccc; background: #111; padding: 8px; border-radius: 4px; }

/* --- Layout --- */
.layout {
  flex:1;
  display:grid;
  grid-template-columns: var(--sidebar-width, 380px) 6px 1fr;
  min-height:0;
  position: relative;
}
.topbar, .bottombar {
  height: 36px;
  background: var(--panel);
  display: flex;
  align-items: center;
  padding: 0 12px;
  border-bottom: 1px solid #2b333f;
  z-index: 40;
  gap: 12px;
}
.bottombar { border-top: 1px solid #2b333f; border-bottom: none; height: 28px; font-size: 12px; }
.brand { font-weight: 700; margin-right: 12px; font-size: 15px; }
.spacer { flex:1; }

/* --- Sidebar Views --- */
.sidebar {
  background: var(--panel);
  border-right:1px solid #2b333f;
  position: relative;
  display: flex; flex-direction: column;
  overflow: hidden;
}
.sidebar-view {
  position: absolute; inset: 0;
  display: flex; flex-direction: column;
  background: var(--panel);
  transition: transform 0.25s cubic-bezier(0.4, 0, 0.2, 1);
  z-index: 10;
}
.sidebar-view.hidden {
  transform: translateX(-100%);
  pointer-events: none;
}
/* Override specific view transitions */
#sidebar-main { z-index: 1; }
#sidebar-main.hidden { transform: translateX(-20%); opacity: 0.5; } /* Эффект глубины */
#sidebar-history { z-index: 2; transform: translateX(0); }
#sidebar-history.hidden { transform: translateX(100%); } /* Выезжает справа налево поверх списка */

/* --- Search & Filters --- */
.search-row { padding:10px; display:flex; gap:8px; align-items:center; }
.search-box { position:relative; flex:1; }
.search-box input {
  width:100%; padding:8px 36px 8px 10px; border-radius:6px;
  border:1px solid #343d4b; background: var(--panel-light); color: var(--text);
}
.search-actions { display: flex; gap: 4px; }
.icon-btn {
  width:32px; height:32px; border:1px solid #3a4351; border-radius:6px;
  background:#2f3744; color:var(--text); cursor:pointer; display:flex; align-items:center; justify-content:center;
  font-size: 14px;
}
.icon-btn:hover { background: #363e4d; }
.icon-btn.active { border-color: var(--accent); background: rgba(76, 175, 80, 0.15); }
.primary.square { background: var(--accent); color:#0f1c0f; font-weight:900; font-size:18px; width:32px; height:32px; padding:0; display:flex; align-items:center; justify-content:center;}

/* --- Unit List --- */
.unit-list { flex:1; overflow-y: auto; position: relative; }
.unit-list-inner { position: relative; width:100%; }
.unit-row {
  position:absolute; left:0; right:0; height: var(--row-h);
  display:grid; grid-template-columns: 1fr 70px 24px; gap:8px; padding:0 12px;
  border-bottom:1px solid #2b333f; cursor:pointer; align-items:center;
  transition: background 0.1s;
}
.unit-row:hover { background:#2b333f; }
.unit-row.is-selected { background:#27313f; border-left: 3px solid var(--accent); padding-left: 9px; }
.unit-name { font-weight:600; font-size:14px; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; display:flex; align-items:center; gap:6px; }
.unit-type-icon { width:16px; height:16px; opacity: 0.7; filter: invert(1); }
.unit-meta { font-size:12px; color:var(--muted); display:flex; align-items:center; gap:6px; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
.dot { width:8px; height:8px; border-radius:50%; display:inline-block; flex-shrink:0; }
.dot.online { background: var(--accent); box-shadow: 0 0 4px rgba(76,175,80,0.4); }
.dot.offline { background: var(--danger); }
.dot.warn { background: #ffa726; }

/* --- Unit Card (Popup) --- */
.unit-card {
  position: absolute; left: 12px; right: 12px; max-width: var(--card-max-width);
  background: var(--panel); border: 1px solid #454f5e; border-radius: 8px;
  padding: 12px; z-index: 50; box-shadow: 0 10px 30px rgba(0,0,0,0.5);
  display: flex; flex-direction: column; gap: 10px;
}
.card-head { display: flex; justify-yourself: space-between; align-items: flex-start; gap: 12px; }
.card-head-main { flex: 1; min-width: 0; }
.card-head-name { font-size: 16px; font-weight: 700; line-height: 1.2; color: #fff; }
.card-head-subtitle { font-size: 12px; color: var(--muted); margin-top: 2px; }

/* ИСПРАВЛЕНО: Кнопки действий в карточке в одну линию */
.card-actions {
  display: flex; gap: 6px; flex-shrink: 0; align-items: center;
}
.card-actions .ghost {
  width: 28px; height: 28px; padding: 0; display: flex; align-items: center; justify-content: center;
  border-radius: 4px; border: 1px solid #3a4351; background: #262e38; font-size: 14px;
}
.card-actions .ghost:hover { background: #303944; border-color: #555; }

.card-status-row {
  background: #1c222c; border: 1px solid #2f3744; border-radius: 6px; padding: 8px 10px;
  display: flex; align-items: center; gap: 10px;
}
.card-status-title { font-weight: 600; font-size: 13px; color: #fff; }
.card-status-caption { font-size: 11px; color: var(--muted); }

/* --- History (Sidebar) --- */
.history-header {
  display: flex; align-items: center; gap: 8px; padding: 12px;
  background: var(--panel); border-bottom: 1px solid #2b333f; flex-shrink: 0;
}
.history-title-block { flex: 1; text-align: center; }
.h-name { font-weight: 700; font-size: 14px; margin-bottom: 4px; }
.h-dates { display: flex; align-items: center; justify-content: center; gap: 4px; }
#hist-date-picker {
  background: #1a1f29; border: 1px solid #3a4351; color: #fff;
  border-radius: 4px; padding: 4px 8px; font-size: 13px; width: 130px;
  text-align: center;
}
#hist-date-picker::-webkit-calendar-picker-indicator { filter: invert(1); cursor: pointer; }

.history-summary {
  display: grid; grid-template-columns: 1fr 1fr; gap: 1px; background: #2b333f; border-bottom: 1px solid #2b333f;
}
.sum-item {
  background: var(--panel); padding: 10px; text-align: center;
  display: flex; flex-direction: column;
}
.sum-val { font-size: 18px; font-weight: 700; color: var(--accent); }
.sum-lbl { font-size: 10px; color: var(--muted); text-transform: uppercase; letter-spacing: 0.5px; }

.history-timeline { flex: 1; overflow-y: auto; background: var(--bg); padding: 10px 0; }
.timeline-item {
  position: relative; padding: 12px 16px 12px 42px;
  border-bottom: 1px solid #252d39; cursor: pointer;
  transition: background 0.15s;
}
.timeline-item:hover { background: #262e38; }
.timeline-item.active { background: #2c3440; }
.timeline-item::before {
  content: ''; position: absolute; left: 20px; top: 0; bottom: 0; width: 2px; background: #343d4b; z-index: 1;
}
.t-icon {
  position: absolute; left: 11px; top: 14px; z-index: 2;
  width: 20px; height: 20px; border-radius: 50%;
  background: var(--panel); border: 2px solid #555;
  display: flex; align-items: center; justify-content: center; font-size: 10px;
}
.timeline-item.trip .t-icon { border-color: #3b82f6; color: #3b82f6; }
.timeline-item.stop .t-icon { border-color: #ef5350; color: #ef5350; }
.t-row { display: flex; justify-content: space-between; align-items: baseline; margin-bottom: 2px; }
.t-time { font-weight: 700; font-size: 13px; color: #fff; }
.t-dur { font-size: 11px; color: var(--muted); background: #1f2631; padding: 1px 5px; border-radius: 4px; border: 1px solid #3a4351; }
.t-detail { font-size: 12px; color: #ccc; }

/* --- Map Overlay (Cursor Info) --- */
.track-info-box {
  position: absolute; top: 15px; left: 60px;
  background: rgba(20, 25, 32, 0.85); backdrop-filter: blur(6px);
  padding: 8px 12px; border-radius: 6px; border: 1px solid #454f5e;
  color: #fff; pointer-events: none; z-index: 1000;
  box-shadow: 0 4px 15px rgba(0,0,0,0.4);
  min-width: 110px; display: flex; flex-direction: column; align-items: center;
}
.track-info-box.hidden { opacity: 0; visibility: hidden; transition: opacity 0.2s; }
.ti-time { font-size: 11px; color: #9ba6b5; margin-bottom: 2px; }
.ti-speed { font-size: 18px; font-weight: 700; color: #4caf50; letter-spacing: -0.5px; }

/* Map Markers */
.map-label-icon { background: none; border: none; }
.arrow-icon {
  font-size: 16px; color: #1e40af; /* Синий */
  text-shadow: 0 0 3px #fff, 0 0 5px #fff; /* Белая обводка для контраста */
  font-weight: 900;
  display: flex; align-items: center; justify-content: center;
  margin-top: -10px; margin-left: -8px;
}

/* Modals */
.modal-overlay { position:fixed; inset:0; background:rgba(0,0,0,0.7); z-index:3000; display:flex; align-items:center; justify-content:center; backdrop-filter: blur(2px); }
.modal { width: 500px; background: var(--panel); border: 1px solid #454f5e; border-radius: 8px; box-shadow: 0 20px 50px rgba(0,0,0,0.6); display: flex; flex-direction: column; }
.modal-header { padding: 15px; border-bottom: 1px solid #343d4b; display: flex; align-items: center; justify-content: space-between; }
.modal-header h3 { margin: 0; font-size: 16px; color: #fff; }
.modal-section { padding: 15px; display: flex; flex-direction: column; gap: 10px; }
.modal-actions { padding: 15px; border-top: 1px solid #343d4b; display: flex; justify-content: flex-end; gap: 10px; }

/* Misc */
.badge { background: #e53935; color: #fff; font-size: 10px; font-weight: 700; padding: 0 5px; border-radius: 10px; margin-left: 4px; min-width: 16px; height: 16px; display: inline-flex; align-items: center; justify-content: center; }
.ghost { background: transparent; border: 1px solid transparent; color: var(--muted); }
.ghost:hover { color: var(--text); background: rgba(255,255,255,0.05); }
